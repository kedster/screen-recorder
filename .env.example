# Cloudflare Pages Environment Variables Template
# Copy this file to .env for local development
# Set these variables in your Cloudflare Pages dashboard for production

# Worker URL for API proxying (REQUIRED)
# Replace 'youraccount' with your actual Cloudflare account subdomain
WORKER_URL=https://screen-recorder-worker.youraccount.workers.dev

# Development settings
NODE_ENV=development

# Optional: Production domain configuration
# CUSTOM_DOMAIN=screen-recorder.yourdomain.com
# API_DOMAIN=api.yourdomain.com